apiVersion: v1
kind: Service
metadata:
  name: {{ include "objectio.osd.fullname" . }}
  labels:
    {{- include "objectio.labels" . | nindent 4 }}
    app.kubernetes.io/component: osd
spec:
  type: ClusterIP
  ports:
    - name: grpc
      port: {{ .Values.osd.service.port }}
      targetPort: grpc
      protocol: TCP
    - name: metrics
      port: {{ .Values.osd.service.metricsPort }}
      targetPort: metrics
      protocol: TCP
  selector:
    {{- include "objectio.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: osd
